# Core runtime dependencies (mirrors [project.dependencies] in pyproject.toml).
# For development installs prefer: pip install -e ".[dev]"
numpy>=1.26
scipy>=1.11
pandas>=2.1
pydantic>=2.5
typer>=0.9
jinja2>=3.1
pyyaml>=6.0
